## Frontend
Proyecto de Frontend con Angular 19

### Servidor de desarrollo
Para iniciar el servidor correr:

```bash
ng serve
```


## Decorators

## Funciones

inject: le pide al injector de Angular que le resuelva y devuelva una instancia de lo que le pases como argumento 
— inject(HttpClient) devuelve la instancia singleton de HttpClient, 
inject(ProductoStoreService) devuelve la instancia singleton de tu servicio

signal: ( equivalente a useState en React )
signal asReadonly: hace un signal solo lectura
